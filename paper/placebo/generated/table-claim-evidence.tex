% GENERATED by pneuma_lab.placebo_paper.render -- do not edit by hand.
\begin{table}[t]
\centering\small
\caption{Claim--evidence matrix. Every claim in the manuscript maps to the receipt that supports it. A claim with no receipt is not made.}
\label{tab:claim-evidence}
\begin{tabular}{llp{0.45\linewidth}}
\toprule
claim & receipt & statement \\
\midrule
\texttt{P1} & \texttt{\result{pending}} & On one frozen open-weight subject across two objective tool-agent settings, correct verifier feedback produced a task-level success gain clearing both the mismatched-report sham and the pooled two-replicate no-feedback condition. \\
\texttt{P2} & \texttt{\result{pending}} & The paired no-feedback branches established an admissible post-trigger resolution scale against which the co-primary estimates were judged. \\
\texttt{C1} & \texttt{\result{pending}} & A four-arm snapshot-paired protocol separating correct task-specific feedback from a mismatched report and from pooled no-feedback continuation, at a tool boundary inside a long-horizon trajectory. \\
\texttt{C2} & \texttt{\result{pending}} & A resampling-null resolution test that turns post-trigger continuation variation into an observed control distribution rather than an assumed zero. \\
\texttt{C3} & \texttt{\result{pending}} & Cross-setting application to repository repair and stateful tool/API work with objective endpoint graders. \\
\texttt{C4} & \texttt{\result{pending}} & A lineage-aware, arm-blind artifact and inference contract reusable by agent benchmark authors. \\
\texttt{C5} & \texttt{\result{pending}} & Preregistered verdicts, including negative and unresolved verdicts, fixed before execution. \\
\texttt{M1} & \texttt{\result{pending}} & REAL and SHAM packets have exactly equal subject-token length under the frozen tokenizer. \\
\texttt{M2} & \texttt{\result{pending}} & A blinded packet classifier could not separate REAL from SHAM above the registered detectability bound. \\
\texttt{M3} & \texttt{\result{pending}} & The arm allocation was drawn before any continuation existed and is reconstructible from the sealed allocation receipts. \\
\texttt{M4} & \texttt{\result{pending}} & The registered power requirement was met at the selected roster tier before the schedule seed was revealed. \\
\texttt{M5} & \texttt{\result{pending}} & The analyst held no arm labels before the hash-gated single-use unblind ceremony. \\
\texttt{N1} & \texttt{---} & This is not a claim that placebo-controlled feedback evaluation is novel; placebo, shape-matched, and compute-matched controls are prior art. \\
\texttt{N2} & \texttt{---} & This is not a claim that verification helps every model or scaffold, nor an identification of a human expert verifier's effect. \\
\texttt{N3} & \texttt{---} & This is not a claim that sham feedback is semantically inert, nor an identification of packet form alone. \\
\bottomrule
\end{tabular}
\end{table}
