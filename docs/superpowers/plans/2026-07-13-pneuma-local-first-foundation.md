# Pneuma Local-First Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the approved 2B-to-4B local foundation program without downloading or training the forbidden 397B reference.

**Architecture:** Extend the existing schema-first, fail-closed Pneuma package with a standalone `pneuma_lab.foundation` package. Keep model execution optional, training authorization explicit, generated artifacts under ignored `build/` storage, and learned-subject reporting independent of the archived evidence ladder.

**Tech Stack:** Python 3.11, PyTorch, Hugging Face Transformers, bitsandbytes NF4, SQLite/FTS5, jsonschema, pytest.

---

### Task 1: Pinned hybrid architecture and recurrent core

- [x] Add failing tests for the revisions, hybrid schedule, derived junctions,
    parameter ceiling, cache preservation, and supervised forecasts.
- [x] Implement the fixed-width shared core and base-specific projections.
- [x] Verify core-disabled parity and real backpropagation.

### Task 2: Falsification, curriculum, resources, and budget

- [x] Add paired four-variant bootstrap gating with the 2-point minimum effect.
- [x] Encode staged tokens, LR selection, checkpoint/resume, and thermal limits.
- [x] Enforce zero default paid compute and one $45 lifetime cloud job.

### Task 3: Data, persistent memory, and local authority

- [x] Govern all ten groups, deduplicate broadly, and keep shards outside data.
- [x] Implement local persistent memory, suppression, hard erasure, and rebuild.
- [x] Implement signed local scopes and deny every high-impact class.

### Task 4: Runtime bridge and durable truth surfaces

- [x] Install validated hooks, transfer the core, preserve caches, and fallback.
- [x] Add authorization, run, profile, and erasure schemas.
- [x] Archive the Level methodology and update canonical status conservatively.
- [x] Run complete repository verification and record hardware-dependent gates.
