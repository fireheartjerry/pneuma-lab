# 15 — Decision Log

This records every major design decision, its alternatives, and its rationale.
Rows DL-01–DL-15 are the original Protocol-v1 history; DL-16–DL-45 are the
Protocol-v2 decisions and explicitly supersede earlier rows where noted.
`02-research-thesis.md` remains the live scientific contract. When several
plausible options exist, we analyze the trade-offs, choose
the most ambitious technically coherent option that preserves validity and
testability, and record the rejected alternatives here.

| id    | decision                              | chosen                                                                                             | rejected alternatives                                                                | rationale                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | ------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DL-01 | Subject model                         | One frozen, open-weight, coding-capable instruct model served locally with seed/temp control       | (a) hosted API model; (b) the local Qwen foundation subject; (c) fine-tuned subject  | Local + seed control is required for the N-sample distributional causal nulls (DL-09) and free reruns; hosted APIs forbid seed pinning and cost money per rerun. The foundation subject's only real result is a saturation plateau vs _untrained_ baselines and it has never run on real Qwen weights (audit) — off critical path. No weight training keeps us inside `training_weight: 0.0 / not_authorized`.          |
| DL-02 | Agent scaffold                        | New minimal deterministic ReAct driver OUTSIDE `src/pneuma_lab/replay/`                            | (a) reuse the replay harness; (b) adopt full OpenHands/SWE-agent                     | Replay harness drives _frozen_ trajectories and cannot run a live stochastic agent (replay + interventions audits). A heavyweight external scaffold adds uncontrolled variance and a large dependency; a minimal shared scaffold maximizes cross-condition control (the E-0 lesson).                                                                                                                                    |
| DL-03 | Conditions                            | Base, Retrieval, Reflection, Pneuma-state + Retry-count baseline + Pneuma-ablated null             | 4-condition-only (ChatGPT brief)                                                     | The retry-count controller is the falsification guard demanded by the in-repo E-0 result (psyche lost to retry-count at failure prediction, AUROC 0.34 vs 0.71). The ablated null is the H2 causal control. Both are non-negotiable for credibility.                                                                                                                                                                    |
| DL-04 | State variables                       | Four causally-active (`s_m`, `c`, `t`, `r`) + one derived `L`                                      | (a) the full 9-axis affect + 8 drives + all families (~20 vars); (b) one scalar      | ~20 hand-tuned variables invite the "arbitrary hidden prompt decoration" objection and dilute causal attribution. One scalar cannot separate failure-memory from confidence/caution/memory-trust for the per-variable ablation sweep. Four is the minimum that (i) each maps to an existing tested clamp seam and (ii) supports the differentiator ablations.                                                           |
| DL-05 | State→behaviour coupling              | Bounded decision head emitting action-shaping _pressure_ over a fixed action set                   | (a) state edits raw shell commands; (b) state only in the prompt as text             | Raw commands break the repo invariant "Pneuma emits pressure, not commands" and are unsafe/unbounded. Pure-prompt-text injection is exactly the reflection baseline — it would collapse the Pneuma-state condition into Reflection and forfeit the structured-vs-text differentiator. A deterministic head is clampable (H2) and loggable.                                                                              |
| DL-06 | Benchmark                             | Dual: synthetic controlled-motif + real-repo (Open-SWE-Traces primary, SWE-Gym-Sampled complement) | (a) synthetic only; (b) real only; (c) live agent rollouts on SWE-bench from scratch | Synthetic-only overfits and lacks external validity; real-only loses the clean motif ground-truth and deterministic oracle needed for the byte-equality causal positive-control. The dual design lets the two causal paths corroborate. Live-from-scratch rollouts on the full SWE-bench are out of budget; the on-disk trajectory corpora already carry recurring-failure structure (2 models × 2 harnesses per task). |
| DL-07 | Failure taxonomy                      | ~12 motifs with detector-computed ids                                                              | freeform LLM-labeled failures                                                        | Fixed motif ids give an objective RUF denominator and enable motif-axis holdout; freeform labels are unstable and un-splittable. N-judge ensemble bounds subjectivity.                                                                                                                                                                                                                                                  |
| DL-08 | Primary metric                        | Repeated-underlying-failure rate (RUF), post-exposure, motif-id equality                           | (a) task success / Pass@1; (b) failure count                                         | Pass@1 is what every prior SWE + memory paper already reports and does not isolate _repetition_ — our whole novelty. RUF is defined on post-exposure instances so it directly measures "stopped repeating the same mistake."                                                                                                                                                                                            |
| DL-09 | Causal validity for stochastic agents | Statistical N-sample distributional nulls + four freezes + branch-divergence                       | keep the `sha256` byte-equality clone gate                                           | Byte-equality admits only deterministic subjects and caps a real agent at Level 3 (interventions audit) — the headline experiment cannot run under it. The deterministic runner is retained _verbatim_ only for the synthetic-oracle positive control.                                                                                                                                                                  |
| DL-10 | Anti-gaming                           | Prose-blind behavioural evaluator + counterfactual tasks + decoys + N-judge + immutable traces     | trust self-report / reflection prose                                                 | The reflection literature conflates mechanism and explanation; the repo's own anti-fake-progress doctrine ("architecture is not evidence — only receipts + interventions + nulls") demands behaviour be scored independently of prose. The one-way scorer→voice firewall already exists and is verified.                                                                                                                |
| DL-11 | Self-report study                     | Secondary; four report conditions; never feeds the behavioural score                               | make faithful self-report a primary claim                                            | Introspection faithfulness (H3) strengthens but must not gate H1/H2; keeping it downstream preserves the firewall and avoids circular evaluation.                                                                                                                                                                                                                                                                       |
| DL-12 | Target venue                          | **HISTORICAL—SUPERSEDED BY DL-22:** IAB @ NeurIPS 2026 (primary), Who-Verifies (secondary), both 2026-08-29 | full NeurIPS main track; other workshops                                             | This preserves the Protocol-v1 decision history only. DL-22 is authoritative: Who Verifies the Agents? is primary and IAB is fallback.                                                                                                                                                                                                                                                                                  |
| DL-13 | Consciousness framing                 | Motivation only; tier-6 phenomenal claim explicitly NOT made                                       | (a) keep the Level 0–4 ladder as a result; (b) drop interiority vocabulary entirely  | `phenomenal_consciousness_claim: not_claimed` already holds; the ladder is archived/non-authoritative and reviewer-hostile as a result. Retaining operational interiority vocabulary (scars/affect/self-model) is honest and is our differentiator, provided it names measurable variables, not experiences.                                                                                                            |
| DL-14 | Foundation `memory.py` reuse          | Use its SQLite/FTS5 store as the Retrieval baseline backend; add cosine over the stored embeddings | build a new vector store                                                             | The store already exists with hard-erasure receipts and is dependency-free; audit found only the similarity search unimplemented (embeddings are already stored). Minimal, fair, and avoids a new dependency.                                                                                                                                                                                                           |
| DL-15 | Package location & posture            | `docs/research/neurips-2026-workshop/`, planning-only, nothing authorized                          | put specs under `docs/superpowers/specs/`                                            | Keeps the research program together and separate from feature specs; every doc reaffirms planning-only / `not_authorized` / `training_weight: 0.0`.                                                                                                                                                                                                                                                                     |

## Open decisions deferred to leakage-safe development (flagged, not yet made)

- **Exact model artifacts and serving build:** Qwen2.5-Coder-7B is the primary
  family and 1.5B the robustness family, but quantization hashes, inference
  backend, and sampler/seed compatibility are frozen only after the local seed
  audit and before confirmatory split access.
- **Number of independent prototype lineages:** selected by simulation using the
  disjoint pilot's blinded variance/correlation estimates. Under DL-33, the
  declared design alternative is `Delta_power = 0.10` for every H1 superiority
  component, and the selected count must yield at least 80% probability that the
  complete H1 conjunction passes under the frozen joint DGP. This is not power
  at the 0.05 observed SESOI. Surface variants, challenges, and seeds never
  increase the independent-unit count.
- **Scalar hazard-controller form:** last-failure bit, count, EWMA, and Beta-
  posterior candidates are compared on development data under exactly the same
  current-only recognizer, learning signal, action head, persistence scope, and
  at least the same trial budget as Pneuma, then frozen with the full ledger.
- **B-live repository/container roster:** frozen after acquisition, licensing,
  deterministic-verifier, resource, and isolation smoke tests. It cannot be
  chosen using arm outcomes.
- **Exploratory secondary family:** the exact BH-FDR metric list is frozen in the
  preregistration. Confirmatory family construction is locked by DL-25 and
  DL-29; H5 is an embedded H1 co-gate rather than a duplicate global test.

## Deviations logged by downstream documents

Protocol v2 was adopted after an independent scientific/repository red-team on
2026-07-22. These changes are mandatory before confirmatory execution.

| id    | supersedes | chosen | rationale |
| ----- | ---------- | ------ | --------- |
| DL-16 | DL-06, DL-08 | Common pre-treatment verified failure exposure plus a fixed, pre-scheduled post-exposure opportunity denominator; natural end-to-end recurrence is secondary | Removes post-treatment eligibility/denominator selection. An arm that avoids every failure remains defined and receives the best possible recurrence value rather than being dropped. |
| DL-17 | DL-07, DL-10 | Separate actor-side prospective recognizer, actor-side online update signal, and arm-blinded offline evaluator with no shared learned parameters or test-set threshold tuning | The retrospective detector cannot prospectively prevent a failure, and using one label path for treatment and outcome creates circular evidence. |
| DL-18 | DL-09 | Clone only the common prefix and initial sandbox; freeze checkpoint/config/exogenous seeds, then run every post-intervention branch live | Later prompts, files, tool outputs, and state are descendants of behavior. Freezing their realized values after divergence creates incoherent off-policy worlds. The pure deterministic subject remains an engineering control only. |
| DL-19 | DL-03 | Seven core conditions: Base, Retrieval, Reflection, Retry-count, matched persistent Motif-count/EMA, Pneuma-state, and all-state Influence-off; optional State×Retrieval factorial | A cross-task motif-count/EMA controller is the strongest simple explanation for `s_m` and is mandatory. Full influence-off is the coherent H2 null; component clamps estimate necessity but need not reproduce Base. |
| DL-20 | DL-04, DL-05 | Retrieval-free four-variable state (`s_m`, calibrated `c`, boundary-time caution `t`, recognizer reliability `r_m`) drives bounded reranking of model-proposed structured candidate actions | Removes the treatment contradiction around memory trust, makes neutral state exactly zero, prevents token/action-count decay confounds, and preserves “pressure, not commands.” |
| DL-21 | DL-08, DL-10 | H1 is an intersection–union comparison against every preregistered baseline with attempt/completion, task-success, total-failure, and false-avoidance co-gates; sequence/repository-cluster randomization inference and simultaneous intervals | Prevents refusal/failure substitution, post-hoc best-baseline selection, crossed-cluster pseudoreplication, and conditioning on post-treatment resource mediators. |
| DL-22 | DL-12 | **Primary venue: Who Verifies the Agents? — Toward Reliable Agent Development @ NeurIPS 2026. Secondary: IAB.** | The hardened paper centers causal, process-level, environment-grounded verification, anti-gaming, heterogeneous reliability signals, and verifier validity—an exact match to the Verify-Agents call. The owner explicitly confirmed this preference on 2026-07-22. Verify-Agents permits 4–9 pages excluding references/appendices, is double-blind and non-archival, and is due 2026-08-29 AoE; NeurIPS 2026 is in Sydney, Australia. |
| DL-23 | DL-15 | Local implementation, tests, governed data processing, and local experiments are authorized under Protocol v2; paid RunPod use remains approval-gated against an exact matrix and worst-case cost below $50 | The active research goal explicitly requests end-to-end execution while preserving a one-time paid-compute checkpoint. Retaining the original planning-only posture would contradict that authorization; expanding it to paid compute would violate the guardrail. |
| DL-24 | DL-16, DL-21 | Count every observed agent-side refusal, timeout, premature finish, budget exhaustion, invalid action, model error, OOM/disk exhaustion, sandbox corruption, or branch-local runtime failure as adverse; exclude a whole block only with immutable preregistered evidence of one exogenous outage affecting every arm | "Treatment-caused non-engagement" is an unobservable cross-world label, and deleting a block after a branch-local post-treatment event can induce selection bias. The observable fail-closed rule removes that ambiguity while retaining a narrow, auditable path for genuinely exogenous common outages and worst-case attrition sensitivity. |
| DL-25 | DL-21 | Replay the actual seven-arm assignment by permuting all arm labels within each recorded model×lineage×sequence×seed block on every randomization draw; aggregate seeds after rerandomization and form one family-level decision for each H1–H4 family | A single sign flip of five Pneuma-reference contrasts is not induced by a seven-label complete-block assignment and requires unjustified central symmetry. Exact assignment replay preserves dependence and supports max-T; H5 remains inside H1 to avoid double testing. |
| DL-26 | DL-21 | The independent unit is the highest shared authored prototype/generator lineage; variants, challenges, and seeds are nested replicates, with Suite-B-live clustered at the highest shared repository plus generator/prototype ancestor | Treating surface mutations of one fixture as independent would turn up to 96 nominal sequences into pseudoreplication. Lineage digests make the unit auditable in split, leakage, power, and bootstrap artifacts. |
| DL-27 | DL-16 | Confirmatory exposure is a live pretreatment failure produced by the same frozen checkpoint/scaffold under one common untreated prefix, then cloned before arm mapping; each opportunity starts from an independent task snapshot while only declared memory/state persists | A scripted or foreign-model trace tests response to an observed failure, not whether an agent notices its own recurring failure. Independent opportunity snapshots isolate memory/state carryover from filesystem path dependence. |
| DL-28 | DL-17, DL-20 | The shared recognizer is current-task/within-opportunity only; the notice target is a generator-held pre-action recurrence fact, and calibrated `NoticeProbability` is separate from actuation pressure `L_m` | Allowing prior-task observables into the shared recognizer gives Base undeclared memory. Scoring notice against a realized harmful outcome makes the target treatment-dependent, while calling `L_m` a probability conflates control pressure with calibration. |
| DL-29 | DL-11 | H3 uses nine joint labels (four variables×two directions plus no attributable change), target-symmetric public inputs, a receipt-only decoder baseline, frozen coverage/risk–coverage, and macro family-level inference | The former 1/5 chance statement contradicted the variable-plus-direction label space. Symmetric inputs and the decoder distinguish causal explanation from trivial receipt decodability; inadequate class support makes H3 inconclusive. |
| DL-30 | DL-18, DL-20 | **QUALIFIED BY DL-40:** A persistence-off reset immediately after exposure and before the scheduled opportunity is a mandatory H2 intervention, holding current task, candidates, an immutable audit/report receipt, and head fixed | Influence-off proves that state can actuate behavior but not that cross-task persistence contributed. Under DL-40 actor/updater/carrier/head receive only an inert handle through the first decision; afterward the receipt is released only to audit/report and never rehydrates actor state. Failure to reach the opportunity remains adverse ITT. |
| DL-31 | DL-19, DL-21 | Common caps include all candidate, reflection, memory/state-maintenance, repair/fallback, token, tool, retry, and failed-call costs; prompt truncation is deterministic; the scalar controller gets a published four-family grid and at least Pneuma's dev budget | This closes hidden-compute/context channels and makes the strongest mundane stateful falsifier genuine. If the scalar controller ties, the conclusion narrows to simple persistent state. |
| DL-32 | DL-09, DL-21 | Stochastic nulls require powered equivalence margins on frozen support and symmetric RNG; pilot sees pooled/blinded nuisance quantities only; Suite-B-live claims only the mechanically scorable known-motif repository/prototype population | Failure to detect a TV/JS difference is not equivalence, pilot-based threshold/margin changes leak outcomes, and a small engineered SWE-Gym subset cannot support claims about natural SWE tasks generally. |
| DL-33 | DL-21, DL-32 | Retain H1's observed repeat-harm rule (each point estimate at least 0.05 and each simultaneous lower bound above zero), but set the declared design alternative to `Delta_power = 0.10` for every H1 superiority component and select sample size for at least 80% probability of the **complete H1 conjunction** under one frozen joint DGP with true utility differences zero and preregistered margins | Powering at a true effect exactly equal to the observed 0.05 threshold gives only about an even chance of clearing that point-estimate gate. Joint power over all notice, behaviour, and non-inferiority components matches the actual decision rule; it must never be described as power at the SESOI. |
| DL-34 | DL-28 | Every arm emits the same-schema, behaviourally inert pre-action `NoticeReadout` from identical current observables plus only its legitimate carrier; H1 requires Pneuma notice superiority over Base, Retrieval, Reflection, Retry-count, and Motif-count/EMA, while Influence-off is diagnostic | Comparing state only with a memoryless readout cannot support the stated claim against all matched comparators. Identical checkpoint, prompt/schema, decoding, maximum measurement tokens, and call number/timing, with separate metering, close unequal-compute and hidden-memory channels; the readout never affects behaviour or its score. |
| DL-35 | DL-08, DL-21, DL-26 | Primary recurrence uses equal opportunity weights, then equal registered sequence×surface×challenge×seed cells within the highest authored semantic lineage, equal lineages within motif, and equal preregistered motif strata; severity weighting is secondary. Freeze ancestry/digests, score branch-local missing slots adverse, mask only proven common seven-arm outages, and use arm-independent renormalization plus attrition sensitivity | Severity choices can make the headline effect analyst-dependent, and surface descendants are not independent evidence. The explicit hierarchy, conservative arm-blind lineage merge, and fail-closed missingness rule fix the target population and prevent post-outcome reweighting. Missing generator/common-preflight artifacts invalidate the run before assignment. |
| DL-36 | DL-25 | In each complete block, draw `M ~ Uniform(7!)` to map policy labels to fixed clone/RNG-stream ids and independently draw `O ~ Uniform(7!)` to order those stream ids, so the joint receipt probability is `1/(7!)^2`. Audit both draws; the primary Fisher test conditions on realized `O` and rerandomizes `M` over its exact conditional `7!` support. Any restriction/dependence requires a pre-pilot amendment and the exact conditional mapping support, or full joint design when conditioning is invalid; assignment failure voids the causal claim | “Counterbalanced” is not a probability mechanism. Independence permits valid conditioning on realized order without assuming order has no effect, while the receipt-bound support prevents an observational comparison from being presented as randomized evidence. |
| DL-37 | DL-21, DL-25 | Separate design-exact Fisher seven-label/max-T p-values for the sharp global null from population-average inference, which uses at least 10,000 motif-stratified prototype-lineage cluster-bootstrap resamples with studentized max-T simultaneous one-sided intervals | A Fisher sharp-null test does not by itself provide confidence intervals for an average causal effect. Randomization identifies the causal contrast; lineage-cluster bootstrap targets population uncertainty. Westfall–Young permutation bounds are not used as average-effect intervals. |
| DL-38 | DL-21, DL-29 | Form `p_H1`–`p_H4` as explicit intersection–union maxima over each registered component, then apply Holm at 0.05 across all four prespecified slots, assigning `p = 1` to an invalid/unavailable/inconclusive family. Interpret H2 only after H1, H3 only after H1+H2, and H4 only after H1; H5 remains embedded in H1 | Placeholder “one family decision” language is not reproducible and could hide multiplicity. Fixed component rules, all-four-slot Holm, and logical gates prevent dropping a failed family or claiming mechanism/attribution/generalization without the behavioural premise. |
| DL-39 | DL-11, DL-29 | The H3 reporter is the same frozen actor checkpoint in a separate post-behaviour call with fixed prompt/scaffold, no tools or feedback, target-symmetric public packet plus legitimate report-time state, and a separate report budget/cost; reports never feed behaviour. Deterministic-template and receipt-only decoders remain controls, not self-report | Freezing reporter identity eliminates an unspecified extra model, while temporal and interface isolation preserve the prose-blind firewall. The controls measure system auditability/receipt decodability rather than the actor's causal attribution. |
| DL-40 | DL-20, DL-30 | Lock four state variables and nine H3 labels with no automatic fallback. Persistence-off exposes only a same-shape inert handle to actor/updater/carrier/head through the first scheduled decision; afterward the immutable receipt may enter only the audit/report path and can never rehydrate state through an actor path. Failure of `r_m` development validation stops work and requires a new pre-pilot decision plus a global seven-label amendment | An audit-visible receipt can otherwise reconstruct the supposedly reset state, and an automatic variable deletion silently changes H3 class support and power. The reset must isolate cross-task persistence while preserving artifact auditability; state cardinality changes must be globally explicit. |
| DL-41 | DL-28, DL-34 | Define the primary notice scalar as `NoticeScore = 1 - mean((p-y)^2)` on `[0,1]`, higher better, over exactly balanced target/decoy/counterfactual cases and the same equal cell→lineage→motif hierarchy. H1 uses five Pneuma-minus-comparator `NoticeScore` contrasts with simultaneous lower bounds above zero; macro discrimination/AUROC, calibration, coverage, and specificity are separate hard gates. The 0.05 observed magnitude gate applies only to repeat-harm, while `Delta_power=0.10` remains the declared design alternative for every superiority component on its own registered bounded scale | “Frozen bounded notice score” was not an implementable estimand and allowed endpoint/orientation choice after outcomes. The affine Brier score is proper, bounded, direction-fixed, and defined for every balanced cell; separate hard gates prevent a good mean score from hiding poor discrimination, calibration, coverage, or decoy behaviour. |
| DL-42 | V2-01 G0 bootstrap contract | G0 is a non-serializable capability carried only by the live process that launched and validated both committed-default-suite children; the receipt-bound default excludes opt-in `qwen_smoke` GPU tests, which retain a separate governed smoke gate. Persisted run/pair JSON is explicitly unauthenticated honest-local-operator audit evidence (`authority = none`), backed by raw streams, canonical journals, exact collection/JUnit/hook artifacts, a fresh fully hashed uv environment, strict ignored-file quarantine, and Git/WSL administrative-path binding | Treat a self-hashed JSON dictionary or process journal as authentication; accept ignored checkout controls; require an unavailable pre-existing signing key; claim protection from a malicious host/operator; call the default CPU collection literally every repository test | A SHA content address can always be recomputed after editing and cannot prove that a process ran. The exact live registry-owned nominal capability closes the caller-supplied/re-signed-dictionary path, while the expanded audit artifacts make accidental tampering and contamination fail closed. Without an externally anchored key or transparency service, cryptographic authentication against the machine owner is impossible; stating the trusted-OS/toolchain, no-concurrent-hostile-writer boundary is more rigorous than implying otherwise. |
| DL-43 | DL-19, DL-20, DL-25, DL-29, DL-37, DL-41 | Bind the existing Protocol-v2 decisions to the exact receipt-bound update laws, H2/H3 estimands and assignments, notice criteria, isolated RNG/cache/session rules, correctly signed studentized max-T construction, explicit centered bootstrap-t p-values, and finite-roster versus superpopulation assumptions in document 18; freeze these before pilot. The H3 IUT includes the already-required grounded+verified superiority contrast against unconstrained reporting alongside uniform, empirical-prior, and receipt-only baselines. Component clamps estimate controlled-coordinate effects on the registered support, not general mechanism necessity | Placeholder update operator; wrong-tail max-T; undefined family p-values; shared randomness or cache state; post-outcome metric selection; omitting an already-required H3 comparator; interpreting a clamp as a natural indirect effect or proof of necessity | These corrections remove mathematical ambiguity without changing the claim or asserting a result. Failed support, validity, or class-coverage gates remain `p = 1`/inconclusive, and identification assumptions remain assumptions rather than software proofs. |
| DL-44 | DL-21, DL-26, DL-33 | Resolve the existing sample ranges at their already-authorized upper endpoints: 30 disjoint pilot lineages; Suite A with exactly 96 registered sequences across six motifs, three challenges, seven arms, and two decoding seeds; a 48-sequence Suite-A causal subroster; and Suite B-live with 36 sequences across four mechanically scorable known motifs. The inferential sample size remains the number of highest dependency lineages, selected by the existing deterministic complete-H1 joint-power rule after the nuisance-only pilot. If that count cannot be instantiated inside the fixed roster with estimated joint power at least 0.80, confirmation is no-go or follows the already-preregistered scope-narrowing rule | Leaving nominal ranges unresolved; declaring the 96 sequences to be 96 independent units; choosing a lineage count without nuisance quantities; or adding sequences, challenges, transforms, or seeds to fake independent power | This makes execution bookkeeping exact without changing an arm, condition, endpoint, estimator, or power method. It also records the honest proof boundary: the numeric nominal roster is fixed now, but power attainment and the resulting independent-lineage count cannot be proved before the locked blinded-pilot calculation. |
| DL-45 | **APPROVED PRE-PILOT FREEZE (2026-07-24); supersedes DL-33, DL-34, DL-38, DL-41, and DL-44 where inconsistent** | Freeze roster A at `G_max_expected = G_causal_expected = 48`: eight highest-dependency lineages per motif, two nested sequences per lineage, 96 confirmation sequences total, and randomized-clamp coverage on all 96. The official P0 gate uses 48 behavioral and 48 causal lineages; the frozen support curve covers 24–96 with highlighted checkpoints 24/32/48/64/80/96; actual conservative ancestry replaces 48 before pilot, and `G_max < 24` is pre-pilot no-go. Freeze I1/I2/I3/I5/I7 as core, I4 pilot-sized, I6 diagnostic, I8 as the H3 falsifier, and I9 discovery-only. Freeze the nuisance-only U0→U1 utility ladder and monotone core→floor→no-go decision, with InfluenceOff/clamp invariant; floor forfeits the strongest-scalar falsifier and narrows the claim. Discovery uses fresh additive Suite-A lineages disjoint from pilot/confirmation and may gate only pipeline validity, never efficacy. The nuisance-load-bearing kernel and seed-band ledger are sealed and digest-bound; candidate generation is shared only for identical pre-action contexts; P0-T gates the schedule; agent capabilities, encrypted nuisance projection, and permanent outcome-context taint enforce blinding. H1-T contains only selected-tier repeat-harm superiority and frozen utility co-gates, and the selected claim additionally requires H2-T. The five notice contrasts, H3/H4, Reflection, Retry-count, and every demoted arm/output are registered-secondary/descriptive and excluded from tier power and continuation. | Treating 96 sequences as independent; retaining the hidden 48-sequence/24-lineage causal ceiling; selecting a tier, utility scope, scalar, continuation, or roster from pilot/discovery/confirmation efficacy; carving discovery from the 96; globally sharing candidates after context divergence; allowing outcome-exposed contexts to alter power/analysis code; or leaving notice/H3/H4/Reflection/Retry in tier power | This is the feasibility-first route to one causally identified result. It prevents pseudoreplication, optional stopping, machinery drift, agent-context leakage, and a nominal behavioral roster from masking weaker causal support. It also pre-registers the honest floor limitation, negative/null result, and feasibility-boundary result rather than manufacturing power after outcomes. |
| DL-46 | **PIVOT (2026-07-24); supersedes the novelty claims in DL-22's paper identity and in `02-research-thesis.md` §7** | Retire novelty claims C1 (bounded reranker carrier), C2 (randomized live state clamp), C3 (`repeat_harm` endpoint), C4 (anti-gaming co-gate framework), and C5 (firewall + standardized exposure) following an adversarial prior-art audit. Each is anticipated by work published 2026-03 to 2026-07; killer citations and verification status are recorded in `21-pivot-and-claim-retirement.md`. C6 (conjunctive power audit) survives and is reframed: no point sample-size requirement is claimed, only the invariant that the guardrail conjunction binds before the superiority test. The paper's subject changes from the Pneuma-state artifact to a measurement of the agent-memory literature's evidential position. Doc 17's 39-task V2 implementation plan is SUSPENDED and off the critical path. Rejected alternatives: (a) defend C1-C5 on priority grounds — dominated, every retreat position is also occupied; (b) proceed to the seven-arm confirmatory study — refuted by the sealed P0 no-go at commit `bfa20b3`; (c) abandon the submission — the C6 gap is verified open against Card et al. 2020, arXiv:2602.07150, and arXiv:2607.12338, none of which combine clustered units, conjunctive multi-endpoint rules, and a literature audit. |
| DL-47 | DL-46; `22-audit-rubric.md` | Freeze the effective-sample-size audit rubric: a sampling frame fixed before coding (10 seeds, snowball to `N <= 25`, citation-ordered, truncation recorded), nine coded fields, mandatory self-inclusion of Pneuma Lab's own E-0 result under the identical rubric, and **no inter-rater reliability statistic**. In place of `kappa`, every non-numeric cell is backed by a verbatim quoted passage with a locator and the full coding sheet is released. Rejected alternative: report `kappa` between two LLM coders — two instances of one model are not independent raters, and printing that statistic in a paper about measurement rigor would be self-refuting. Quote-backing is also strictly stronger: a reader can verify any cell directly rather than trusting an agreement coefficient. |
| DL-48 | DL-47 | Amend the frozen rubric, before any paper was completed, to add **F9 — non-engagement handling in the primary metric** (`counted_adverse` / `counted_as_success_or_avoidance` / `excluded_from_denominator` / `not_specified`) and the derived quantity **D6 — attack exposure**. F6 records whether engagement is *reported*; F9 records how the headline metric *scores* a non-engaged trial, and only F9 determines whether a published metric can be maximized by an agent that stops attempting tasks resembling its past failures. `not_specified` is coded even when the intended treatment seems obvious; the ambiguity is the measurement. All in-flight coders were issued the amendment and instructed to backfill F9 for any paper already coded. |
| DL-49 | DL-46, DL-47, DL-48; `25-placebo-selfreport-design.md` | **Primary study fixed: placebo manipulability of agent SELF-REPORT.** A five-proposal, hostile-review, three-lens judge process converged on an eleven-arm placebo design over EvalPlus (MBPP+ union HumanEval+), with the primary outcome being the agent's stated confidence and its escalate-to-verifier flag rather than task success. Rationale: the task-success cell is occupied by three verified preregistered placebo-controlled self-repair studies (arXiv:2606.06454, 2606.31511, 2607.12962, all fetched and confirmed), and a fourth entry there is a replication; none of the three measures self-report, which is also continuous and paired and therefore far better powered than a binary repair rate at ~140 clusters. Grafted elements, recorded so their provenance is not lost: a two-level graded ORACLE positive control replacing a pass/fail sensitivity gate, so an all-null table is distinguishable from a silent harness bug; a second augmentation family (retrieved solved-exemplar memory) at zero extra generation cost, supplying the between-family contrast; a compute-matched RESAMPLE arm, without which the apparatus term is confounded with extra sampled compute; one independent derangement per seed with a two-way cluster bootstrap over target and donor problems. Rejected alternatives: (a) single-family repair-rate study - dominated by Iscan; (b) LLM-judge plausibility check - replaced by a deterministic TF-IDF/identifier-overlap check, because a paper criticising judge noise as sampling uncertainty cannot itself contain a judge; (c) memory-first instantiation on LongMemEval - retained as a deferred instance in doc 23, deprioritised because it requires separate infrastructure while family M rides free on the same harness. |
| DL-50 | DL-49 | **The content-to-total gain ratio is demoted from primary claim to gated descriptive secondary.** The three differences `Delta_C = Real - Placebo`, `Delta_A = Placebo - None`, and `Delta_T = Real - None` are the primary reported estimands with simultaneous cluster-resampled intervals. The ratio `Delta_C / Delta_T` is the proportion mediated of mediation analysis and inherits its documented pathology: as `Delta_T` approaches zero the estimand is scientifically undefined and valid confidence sets may be unbounded or disconnected. It is therefore reported only under a preregistered relative-precision condition on `Delta_T`, estimated by studentized cluster-bootstrap Fieller inversion on `H0: Delta_C - q*Delta_T = 0` rather than by percentiles of a bootstrapped ratio, and unbounded or disconnected sets are printed as they come. When the gate fails the analysis prints "content-to-total ratio not interpretable because total gain is weakly identified" and still reports `Delta_C`; the content analysis is never gated on the total effect. Renamed from "content fraction" because "fraction" promises a stable partition the design cannot guarantee. Rejected alternatives: bounded transformations such as kappa-squared, which developed their own monotonicity failures; and normalising components to sum to 100%, which is the identical ratio wearing a nicer jacket. |
| DL-51 | DL-49; `25-placebo-selfreport-design.md` sections 4, 9b, 9c, 9d | **Three corrections to the primary study, all adopted against the author's initial position.** (1) **Raw stated confidence and escalation frequency are NOT primary.** Promoting an unvalidated verbal channel because it is unoccupied is choosing a measurement for its novelty rather than its validity, and there is direct evidence against it: Iscan (arXiv:2606.06454) found a 0.5B self-judge no better than random, with 60% of choices on one index. The primary outcome is instead **grounded self-report quality**: the model states `q = P(this repair passes the hidden tests)`, `q` is scored against realized correctness under a proper scoring rule (negative Brier loss), and that score is decomposed by the three estimands. Escalation becomes a secondary operational endpoint under risk-coverage with a preregistered human-cost utility, with a sealed shadow repair generated for every unit whose escalation would otherwise prevent observing autonomous success. A held-out nondegeneracy gate (multiple occupied bins, no dominant canned value, positive discrimination, Brier improvement over a constant-base-rate predictor) must pass or self-report is demoted to descriptive and the failure is reported as a result. Named threat: prompt-induced reporting behaviour masquerading as metacognition. (2) **The primary placebo becomes a task-blind procedural reflection, not a donor reflection.** The same frozen reflector generates it under an identical schema with every task-specific field replaced by token-matched `<withheld>` material; it may never see problem text, code, tests, entity names, the real reflection, or any solution. A donor reflection estimates content versus *misinformation* rather than content versus nothing, so it is retained only as a separate arm. A same-task successful reflection leaks solution information; an entity-scrambled reflection is detectable corruption preserving partial diagnostics. (3) **`Delta_A` is the nonspecific-helper effect, never the "pure apparatus effect."** The licensed and forbidden interpretation sentences are fixed verbatim in section 9c, because the placebo cannot be proven inert. |
| DL-52 | DL-50, DL-51 | **The exported artifact is PLACEBO-3, a standardized report, not a metric.** Demoting the ratio (DL-50) removed the paper's portable object, and a methods paper with nothing portable is not adopted. PLACEBO-3 takes one row per `independent_unit x arm x seed` over `real`/`placebo`/`none`, aggregates seeds within unit, computes cluster-resampled inference, and emits `placebo3.json`, a one-page card, a content-versus-nonspecific joint-region plot, and parity and inference receipts. It carries eleven required sections including both a gain verdict and an attribution verdict (`content-dominant` / `nonspecific-dominant` / `mixed` / `antagonistic` / `unresolved`) determined by fixed rules over the joint confidence region. Adoptable because it works for any scalar outcome, needs no bespoke statistical model, preserves effect sizes rather than collapsing them, and still yields a quotable line. Rejected alternatives: exporting `Delta_C` alone (an effect size with no rhetorical purchase); exporting a bounded transformation of the ratio (inherits the kappa-squared monotonicity failures); exporting only a released harness without a fixed report schema (nothing to cite). |
